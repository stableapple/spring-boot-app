pipeline {
  agent {
    docker {
      image 'ri25/maven-docker:latest'
      args '-v /var/run/docker.sock:/var/run/docker.sock -u 0:0'
    }
  }

  stages {

    stage('Checkout') {
      steps {
        sh 'echo passed'
        git branch: 'master', url: 'https://github.com/stableapple/spring-boot-app.git'
      }
    }

    stage('Build and Test') {
      steps {
        sh '''
          ls -ltr
          mvn clean package
        '''
      }
    }

    stage('Static Code Analysis') {
      steps {
        withSonarQubeEnv('SonarQube') {
          sh 'mvn clean verify sonar:sonar'
        }
      }
    }

    stage('Build and Push Docker Image') {
      environment {
        DOCKER_IMAGE = "ri25/ci-cd:${BUILD_NUMBER}"
        REGISTRY_CREDENTIALS = credentials('docker-cred')
      }
      steps {
        script {
          sh 'docker build -t ${DOCKER_IMAGE} .'
          def dockerImage = docker.image("${DOCKER_IMAGE}")
          docker.withRegistry('https://index.docker.io/v1/', 'docker-cred') {
            dockerImage.push()
          }
        }
      }
    }

    stage('Update Deployment File') {
      environment {
        GIT_REPO_NAME = "spring-boot-app"
        GIT_USER_NAME = "stableapple"
      }
      steps {
        withCredentials([string(credentialsId: 'github', variable: 'GITHUB_TOKEN')]) {
          sh '''
            cd $WORKSPACE
            echo "Workspace content before git update:"
            ls -la
            git config user.email "riddhi01.menon@gmail.com"
            git config user.name "stableapple"

            sed -i "s/replaceImageTag/${BUILD_NUMBER}/g" deployment.yml

            git add deployment.yml
            git commit -m "Update deployment image to version ${BUILD_NUMBER}"
            git push https://${GITHUB_TOKEN}@github.com/${GIT_USER_NAME}/${GIT_REPO_NAME} HEAD:master
          '''
        }
      }
    }

  }
}
